# bedrock-adb 1.2.1+1.21 使用说明（Bedrock Allow Deny Border）

一个 Fabric **客户端** Mod，在 Java 版（Minecraft 1.21）中复刻基岩版教学用
**允许方块 / 拒绝方块 / 边界方块**，用于局域网（LAN）联机合作与地图制作。

**1.2.0 新增内容**

- 「边界方块模式」配置项：可切换 **仿基岩版** 与 **拦截所有实体** 两种通行规则。
- ModMenu + Cloth Config **可视化配置界面**（原命令配置方式完整保留）。
- 配置改动会实时推送给局域网内的所有成员，无需重进世界。

**1.2.1+1.21 修复（本版）**

- **边界方块整列全高度阻挡**：阻挡不再是"方块自身那一格"，而是覆盖边界方块
  所在 (x,z) 列的**所有 Y 值**（世界底部 → 顶部）。不满足通行条件的实体无论
  垫高跳跃、飞行、鞘翅，还是从下方挖隧道，都无法从边界方块的上方或下方跨过。
- 拦截所有实体模式下，箭/末影珍珠/火球等高飞投射物也无法从高空越过边界列
  （特权玩家自己射出的投射物放行）。
- 通行豁免规则不变：旁观者、白名单 / OP(+创造) 特权玩家、方块级白名单玩家
  仍可穿过（穿越时不受 Y 限制）。

---

## 一、安装要求

| 类别 | 内容 | 是否必需 |
|---|---|---|
| 游戏版本 | Minecraft Java 1.21 | 必需 |
| 加载器 | Fabric Loader ≥ 0.19.4 | 必需 |
| Fabric API | 任意 1.21 版本 | 必需 |
| 本 Mod | `bedrock-adb-1.2.1+1.21.jar` → 放入 `mods/` | 必需 |
| Mod Menu | 11.0.x（Modrinth 下载） | **可选**（提供"配置"按钮入口） |
| Cloth Config | 15.0.140（Modrinth 下载） | **可选**（渲染配置界面） |

> - 只装本 Mod：一切功能正常，只是没有图形配置界面，请用第四章的命令配置。
> - 想用图形界面：`Mod Menu` 与 `Cloth Config` **两个都要装**；缺任一个时，
>   配置按钮会自动隐藏、不会报错。
> - **联机成员必须全部安装本 Mod 的同一版本**（客户端 Mod，规则在每台机器上
>   共同判定；主机之外的人无法修改配置，只能接收同步）。

---

## 二、图形配置界面（Mod Menu + Cloth Config）

1. 打开游戏（单人游戏或"对局域网开放"后均可）。
2. 按 `Esc` → 「Mods…」（Mod Menu 的模组列表）。
3. 左侧列表找到 **Bedrock Allow Deny Border**。
4. 右侧点击 **配置（Config）** 按钮，打开配置页。
5. 修改后点击右下角「保存并退出」，配置写入
   `config/bedrock-adb.json` 并**立即同步给局域网内所有玩家**。

配置页内每个选项带说明悬浮提示（中文界面下自动显示中文）。

---

## 三、配置项说明

| 配置项 | 取值 | 默认 | 说明 |
|---|---|---|---|
| 边界方块模式 | 仿基岩版 / 拦截所有实体 | 仿基岩版 | 见下方矩阵 |
| 特权玩家必须处于创造模式 | 开 / 关 | 开 | 开启：需 **2 级权限且创造模式** 才能绕过规则；关闭：仅需 2 级权限 |
| 管理员（OP）可绕过规则 | 开 / 关 | 开 | 关闭后即使 OP 也无法豁免（白名单成员不受此开关影响） |
| 显示边界方块粒子特效 | 开 / 关 | 开 | 在附近边界方块上渲染红色尘埃，便于看到隐形墙 |

### 「特权玩家」定义（两种模式通用）

满足任一条件即视为特权玩家，可穿过边界方块，并可放置/破坏允许、拒绝、边界方块：

1. 在 `/allowdeny whitelist` 全局白名单中；
2. 该**单个边界方块**的 NBT 白名单中（地图作者通过物品数据组件写入）；
3. OP（2 级权限），按上方「创造模式」开关决定是否还需创造模式。

### 边界方块模式对照表（核心）

| 通行实体 | 仿基岩版（默认） | 拦截所有实体 |
|---|---|---|
| 非特权玩家 | 🚫 挡 | 🚫 挡 |
| 怪物 / 动物（生物） | 🚫 挡 | 🚫 挡 |
| 特权玩家（白名单 / OP+创造） | ✅ 放行 | ✅ 放行 |
| 旁观者 | ✅ 放行 | ✅ 放行 |
| 箭、三叉戟等投射物 | ✅ 放行 | 🚫 挡 |
| 掉落物 / 经验球 / 船车等实体 | 🚫 挡 | 🚫 挡 |
| 恼鬼、末影龙（不可控实体） | ✅ 放行 | 🚫 挡 |

> 说明：允许/拒绝方块的行为不受此开关影响（仅影响边界方块的通行判定）。
> 「拦截所有实体」模式下，自己射出的箭也会被自己的边界墙挡住——这是预期行为。
> **几何规则（1.2.1+1.21）**：边界列覆盖整列所有 Y——垫高跳跃、飞行/鞘翅、从下方
> 挖隧道均无法绕过；想要留门请使用命令把对应玩家加入白名单或拆除该段方块。
> 已知边界：主流的投射物（箭/三叉戟/末影珍珠/雪球/火球/恼鬼弹等）均已拦截；
> 个别非标准自定义飞行实体（如旋风人风弹、钓鱼浮标）不在拦截范围内。

---

## 四、命令配置（无 ModMenu / Cloth 时使用）

所有命令需 **2 级权限**（OP），命令前缀为 `/allowdeny`：

| 命令 | 作用 |
|---|---|
| `/allowdeny config borderMode bedrock_like` | 边界方块切为仿基岩版模式 |
| `/allowdeny config borderMode block_all_entities` | 边界方块切为拦截所有实体 |
| `/allowdeny config requireCreative <true/false>` | 特权是否需要创造模式 |
| `/allowdeny config opBypass <true/false>` | OP 是否可绕过规则 |
| `/allowdeny config showBorderEffect <true/false>` | 边界粒子特效开关 |
| `/allowdeny reload` | 从磁盘重载 `config/bedrock-adb.json` 并同步 |
| `/allowdeny whitelist add <玩家名>` | 加入全局白名单（忽略大小写） |
| `/allowdeny whitelist remove <玩家名>` | 移出全局白名单 |
| `/allowdeny whitelist list` | 查看白名单 |
| `/allowdeny whitelist clear` | 清空白名单 |
| `/allowdeny query <x y z>` | 查询该位置被哪个允许/拒绝方块管辖 |

> 图形界面改配置 = 执行命令改配置 + 写盘，二者完全等价。

---

## 五、配置文件（手动编辑）

路径：`config/bedrock-adb.json`（游戏根目录下）。字段：

```jsonc
{
  "requireCreative": true,          // 特权是否必须创造模式
  "opBypass": true,                 // OP 是否可绕过
  "showBorderEffect": true,         // 边界粒子特效
  "borderBlockMode": "BEDROCK_LIKE" // 或 "BLOCK_ALL_ENTITIES"
}
```

- 旧版（1.1.3）配置文件**没有** `borderBlockMode` 字段，升级后自动按
  「仿基岩版」处理，无需手动修改。
- 手改后执行 `/allowdeny reload` 生效；非法取值会自动回落为默认值。

---

## 六、常见问题（FAQ）

**Q1：Mods 列表里没有「配置」按钮？**
A1：Mod Menu 与 Cloth Config 缺一不可（见安装要求）；按钮缺失不报错，请改用
第四章命令配置。

**Q2：改成「拦截所有实体」后朋友那边没效果？**
A2：确认全员使用 1.2.0 同一版本；配置由主机（开房者）修改，改动会自动同步，
但**客人本机**的 ModMenu 界面只读镜像，请让主机修改。

**Q3：换模式后以前放的边界方块会变吗？**
A3：不会，方块无需更换——通行规则即时按新模式判定（客户端碰撞 + 服务端判定
同步生效）。

**Q4：边界方块"挡不住"？从上面跳过去 / 下面挖洞过去了？**
A4：请确认你的版本是 **1.2.1+1.21**（1.2.0 及更早只挡方块自身一格高度）。另外，
OP（2 级权限，单机默认即是）+ 创造模式默认视为"特权玩家"，会直接穿过——
测试阻挡请用生存/冒险模式且不在白名单的账号，或先执行
`/allowdeny config opBypass false`。

**Q5：末影珍珠 / 箭能穿过边界吗？**
A5：仿基岩版模式下投射物可穿过（基岩版行为）。拦截所有实体模式下不行：
投射物会在边界列面前停下；特权玩家自己射出的投射物仍可穿过。

**Q6：这个 Mod 能用于专用服务器（Dedicated Server）吗？**
A6：不能。它是 `environment: client` 的 Mod，专为单人 / 局域网主机合作设计；
请勿放入专用服务器 mods 目录。

**Q7：造图时怎么让某人只过某一段边界？**
A5：边界方块的单个方块级白名单：用 `/give` 写入 `bedrock_adb:whitelist`
数据组件（字符串数组，玩家名）后放置该方块，名单内玩家可穿过此方块。

---

## 七、开发者：从源码构建

```powershell
# 工程位于仓库根目录；需要 JDK 21+ 与本机 Gradle 9.5.0
gradle build
# 产物: build/libs/bedrock-adb-1.2.0.jar
```

版本清单（`gradle.properties`）：MC 1.21 / Yarn 1.21+build.9 / Loom 1.17.20 /
Loader 0.19.5 / Fabric API 0.102.0+1.21 / Cloth Config 15.0.140 /
Mod Menu 11.0.4。核心源码结构：

```
src/main/java/dev/bedrockadb/
  config/    ModConfig(JSON) · BorderBlockMode(枚举) · ModConfigScreen(Cloth界面) · ModMenuIntegration
  block/     BorderBlock(通行判定) · AllowDenyBlock · UtilityBlock …
  rule/      RuleChecker · RegionRule · Privilege · WhitelistSource(s)
  state/     WhitelistPersistentState(存档白名单) · ClientSyncState(联机镜像)
  net/       WhitelistSyncPayload(S2C同步, 含 borderBlockMode)
  registry/  ModBlocks/Items/BlockEntities/DataComponents/ItemGroups/Networking/Commands
```

---

*许可证：MIT。详情见 jar 内 LICENSE_bedrock-adb。*
